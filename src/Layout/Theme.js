import { createTheme } from '@mui/material/styles';

const mainColor = "#075E54"

const theme = createTheme({
    palette: {
        primary: {
            main: mainColor
        }
    }
});

export default theme